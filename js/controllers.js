'use strict';

/* Controllers */

var ElControllers = angular.module('ElControllers', []);

ElControllers.controller('WebSiteCtrl', ['$scope', '$http',
  function($scope, $http) {
  }]);

ElControllers.controller('PriceCtrl', ['$scope', '$routeParams',
  function($scope, $routeParams) {
  }]);
